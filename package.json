{
  "name": "eas-local-cache",
  "version": "1.0.4",
  "description": "A custom build cache provider for Expo CLI. Uses local to store builds.",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "format": "prettier --write \".github/**/*.{yml,yaml}\" \"scripts/**/*.{js,mjs}\" \"src/**/*.{ts,tsx}\" \"test/**/*.{js,mjs,ts,tsx}\" package.json",
    "format:check": "prettier --check \".github/**/*.{yml,yaml}\" \"scripts/**/*.{js,mjs}\" \"src/**/*.{ts,tsx}\" \"test/**/*.{js,mjs,ts,tsx}\" package.json",
    "lint": "oxlint --deny-warnings src test",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "bun test",
    "prepack": "bun run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dennytosp/eas-local-cache.git"
  },
  "keywords": [
    "expo",
    "expo-cache",
    "eas-cache",
    "build-cache",
    "buildCacheProvider",
    "expo-cli",
    "react-native"
  ],
  "author": "mad.dinh",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dennytosp/eas-local-cache/issues"
  },
  "homepage": "https://github.com/dennytosp/eas-local-cache#readme",
  "devDependencies": {
    "@expo/config": "^57.0.7",
    "@types/bun": "^1.3.14",
    "expo-module-scripts": "^56.0.3",
    "oxlint": "1.78.0",
    "prettier": "3.9.6",
    "typescript": "^7.0.2"
  },
  "files": [
    "build",
    "src",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
